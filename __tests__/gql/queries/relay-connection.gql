query TestRelayConnection(
  $first: Int
  $last: Int
  $after: String
  $before: String
) {
  testRelayConnection(
    first: $first
    last: $last
    after: $after
    before: $before
  ) {
    edges {
      cursor
      node {
        id
      }
    }
    pageInfo {
      hasPreviousPage
      hasNextPage
      startCursor
      endCursor
    }
  }
}
